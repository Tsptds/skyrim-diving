#pragma once
namespace plugin {
    class Compatibility {
        public:
            inline static bool SkyParkour = false;

            Compatibility() = default;
            ~Compatibility() = default;

        private:
    };
}  // namespace plugin